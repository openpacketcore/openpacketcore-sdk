[package]
name = "opc-linux-route-sys"
version.workspace = true
authors.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Narrow Linux rtnetlink route and rule UAPI boundary for OpenPacketCore"
readme = "README.md"
documentation = "https://docs.rs/opc-linux-route-sys"
keywords = ["5g", "route", "netlink", "linux", "packet-core"]
categories = ["network-programming", "os::linux-apis"]
publish = false

[dependencies]
libc = "0.2"

[lints.rust]
unsafe_code = "allow"
unsafe_op_in_unsafe_fn = "deny"
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(opc_linux_route_sys_force_unsupported)'] }
