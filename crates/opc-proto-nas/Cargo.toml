[package]
name = "opc-proto-nas"
version.workspace = true
authors.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "NAS-5GS (TS 24.501) v1 codec: framing, 5GS mobile identity, BCD unpacking, and Registration Request/Accept IE parsing"
readme = "README.md"
documentation = "https://docs.rs/opc-proto-nas"
keywords = ["5g", "nas", "packet-core", "codec", "telecom"]
categories = ["network-programming", "encoding"]
publish = false # experimental: not published until graduation

[dependencies]
opc-protocol = { path = "../opc-protocol", version = "0.2.0" }
bytes = { workspace = true }

[dev-dependencies]
quickcheck = "1"

[lints]
workspace = true
