[package]
name = "opc-mgmt-audit-store"
version.workspace = true
publish = false # source-build only while the opc-persist #143 publication gate remains open
authors.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Durable tamper-evident management audit sink for OpenPacketCore"
readme = "README.md"
documentation = "https://docs.rs/opc-mgmt-audit-store"
keywords = ["5g", "cnf", "audit", "sqlite", "security"]
categories = ["network-programming"]

[dependencies]
opc-config-model = { path = "../opc-config-model", version = "0.2.0" }
opc-mgmt-audit = { path = "../opc-mgmt-audit", version = "0.2.0" }
opc-persist = { path = "../opc-persist", version = "0.2.0" }
thiserror = { workspace = true }
tokio = { workspace = true }

[dev-dependencies]
rusqlite = "0.32"
tempfile = "3"

[lints]
workspace = true
