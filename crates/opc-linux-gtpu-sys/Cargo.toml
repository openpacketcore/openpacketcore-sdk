[package]
name = "opc-linux-gtpu-sys"
version.workspace = true
authors.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Narrow Linux GTP-U rtnetlink and generic-netlink UAPI boundary for OpenPacketCore"
readme = "README.md"
documentation = "https://docs.rs/opc-linux-gtpu-sys"
keywords = ["5g", "gtpu", "gtp", "linux", "packet-core"]
categories = ["network-programming", "os::linux-apis"]
publish = false

[dependencies]
libc = "0.2"

[lints.rust]
unsafe_code = "allow"
unsafe_op_in_unsafe_fn = "deny"
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(opc_linux_gtpu_sys_force_unsupported)'] }
